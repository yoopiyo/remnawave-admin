# 🤖 Remnawave Admin Bot

<div align="center">

**Telegram-бот для управления панелью Remnawave**

[![Docker](https://img.shields.io/badge/Docker-Ready-blue)](https://www.docker.com/)
[![Python](https://img.shields.io/badge/Python-3.12+-green)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)

[English](README_EN.md) | [Русский](README.md)

</div>

---

## ✨ Возможности

### 👥 Управление пользователями
- 🔍 Поиск пользователей по username, email, Telegram ID, описанию
- ➕ Создание пользователей с пошаговым вводом
- ✏️ Редактирование профиля (трафик, лимиты, контакты, сквады)
- 💻 Управление HWID устройствами (добавление, удаление, лимиты)
- 📊 Статистика пользователя (трафик, история подписки, использование нод)
- 🔄 Массовые операции с пользователями

### 🛰 Управление нодами
- 📋 Просмотр списка нод с реальным временем
- 🔄 Включение/выключение нод
- 🔁 Перезапуск нод
- 📊 Мониторинг трафика и использования
- ⚙️ Назначение профилей конфигурации
- 📈 Статистика по нодам

### 🖥 Управление хостами
- 📋 Просмотр списка хостов
- ➕ Создание и редактирование хостов
- 🔄 Массовые операции

### 🧰 Ресурсы
- 📑 **Шаблоны** - создание и управление шаблонами подписок
- ✂️ **Сниппеты** - управление конфигурационными сниппетами
- 🔑 **API токены** - управление токенами доступа
- 📄 **Конфиги** - просмотр конфигураций

### 💰 Биллинг
- 📜 История платежей
- 🏢 Управление провайдерами
- 🖥 Управление биллинг-нодами
- 📊 Статистика по биллингу

### 📊 Статистика и мониторинг
- 📈 Статистика панели (пользователи, ноды, хосты)
- 🖥 Статистика сервера (CPU, память, аптайм)
- 📶 Статистика трафика
- 🔔 Уведомления о событиях через webhook

### 🌐 Дополнительно
- 🌍 Поддержка русского и английского языков
- 🔔 Webhook-уведомления о событиях (создание, изменение, удаление пользователей)
- 🔐 Безопасная аутентификация webhook через HMAC-SHA256
- 🎨 Интуитивный интерфейс с inline-кнопками
- 🐳 Готов к развертыванию через Docker Compose

---

## 🚀 Быстрый старт

### 📋 Требования

- **Docker** и **Docker Compose** (рекомендуется)
- Или **Python 3.12+** (для локальной разработки)
- Токен Telegram-бота от [@BotFather](https://t.me/BotFather)
- Токен доступа к API Remnawave

### 🔧 Установка

#### 1. Клонируйте репозиторий

```bash
git clone https://github.com/case211/remnawave-admin.git
cd remnawave-admin
```

#### 2. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
nano .env
```

**Обязательные переменные:**

```env
# Telegram Bot
BOT_TOKEN=ваш_токен_telegram_бота

# Remnawave API
API_BASE_URL=http://remnawave:3000  # Для Docker-сети
# или
API_BASE_URL=https://ваш-домен-панели.com/api  # Для внешнего API
API_TOKEN=ваш_токен_api

# Администраторы
ADMINS=123456789,987654321  # ID администраторов через запятую

# Локализация
DEFAULT_LOCALE=ru  # ru или en
LOG_LEVEL=INFO
```

**Опциональные переменные:**

```env
# Уведомления в Telegram
NOTIFICATIONS_CHAT_ID=-1001234567890  # ID группы/канала
NOTIFICATIONS_TOPIC_ID=123  # ID топика (опционально)

# Webhook (для получения уведомлений от панели)
WEBHOOK_SECRET=ваш_секретный_ключ  # Должен совпадать с WEBHOOK_SECRET_HEADER в панели
WEBHOOK_PORT=8080  # Порт для webhook сервера
```

> 💡 **Совет:** Получите свой Telegram ID, отправив сообщение [@userinfobot](https://t.me/userinfobot)

#### 3. Разверните с помощью Docker Compose

```bash
# Создайте Docker-сеть (если еще не создана)
docker network create remnawave-network

# Запустите бота
docker compose pull
docker compose up -d

# Проверьте логи
docker compose logs -f bot
```

#### 4. Настройте webhook в панели Remnawave

Подробная инструкция по настройке webhook доступна в [WEBHOOK_SETUP.md](WEBHOOK_SETUP.md)

**Кратко:**
1. В панели Remnawave укажите URL webhook: `http://bot:8080/webhook` (для Docker) или `https://ваш-домен-бота.com/webhook` (для внешнего)
2. Установите `WEBHOOK_SECRET_HEADER` в панели равным `WEBHOOK_SECRET` в боте

---

## 💻 Локальная разработка

### 1. Создайте виртуальное окружение

```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте окружение

```bash
cp .env.example .env
nano .env
```

Для локальной разработки используйте:
```env
API_BASE_URL=https://ваш-домен-панели.com/api
```

### 4. Запустите бота

```bash
python -m src.main
```

---

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Обязательна | По умолчанию | Описание |
|------------|-------------|---------------|-----------|
| `BOT_TOKEN` | ✅ Да | - | Токен Telegram-бота от @BotFather |
| `API_BASE_URL` | ✅ Да | - | Базовый URL API Remnawave |
| `API_TOKEN` | ✅ Да | - | Токен аутентификации API |
| `ADMINS` | ✅ Да | - | Список ID администраторов через запятую |
| `DEFAULT_LOCALE` | ❌ Нет | `ru` | Язык по умолчанию (`ru` или `en`) |
| `LOG_LEVEL` | ❌ Нет | `INFO` | Уровень логирования (`DEBUG`, `INFO`, `WARNING`, `ERROR`) |
| `NOTIFICATIONS_CHAT_ID` | ❌ Нет | - | ID группы/канала для уведомлений |
| `NOTIFICATIONS_TOPIC_ID` | ❌ Нет | - | ID топика в группе (для форумов) |
| `WEBHOOK_SECRET` | ❌ Нет | - | Секретный ключ для проверки webhook (HMAC-SHA256) |
| `WEBHOOK_PORT` | ❌ Нет | `8080` | Порт для webhook сервера |

### Docker-сеть

Бот требует доступа к Docker-сети `remnawave-network`. Если она не существует, создайте её:

```bash
docker network create remnawave-network
```

---

## 📱 Команды бота

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота и показать главное меню |
| `/help` | Показать справку по командам |
| `/health` | Показать статус здоровья системы |
| `/stats` | Показать статистику панели и сервера |
| `/bandwidth` | Показать статистику трафика |

### Команды для работы с пользователями

| Команда | Описание |
|---------|----------|
| `/user <username\|telegram_id>` | Просмотр информации о пользователе |
| `/user_create <username> <expire_iso> [telegram_id]` | Создать нового пользователя |

### Команды для инфраструктуры

| Команда | Описание |
|---------|----------|
| `/node <uuid>` | Просмотр информации о ноде |
| `/host <uuid>` | Просмотр информации о хосте |
| `/sub <short_uuid>` | Открыть ссылку на подписку |

### Навигация по меню

Бот использует inline-клавиатуры для навигации. Основные разделы:

- **👥 Пользователи** - Управление пользователями, поиск, создание, редактирование, статистика, HWID
- **🛰 Ноды** - Управление и мониторинг нод, статистика трафика
- **🖥 Хосты** - Управление хостами, массовые операции
- **🧰 Ресурсы** - Шаблоны, сниппеты, API токены, конфиги
- **💰 Биллинг** - История платежей, провайдеры, биллинг-ноды
- **📊 Система** - Здоровье системы, статистика, управление

---

## 📁 Структура проекта

```
remnawave-admin/
├── src/
│   ├── main.py                 # Точка входа приложения
│   ├── config.py               # Управление конфигурацией
│   ├── handlers/               # Обработчики событий
│   │   ├── basic.py            # Базовые обработчики
│   │   ├── commands.py         # Обработчики команд
│   │   ├── users.py            # Управление пользователями
│   │   ├── nodes.py            # Управление нодами
│   │   ├── hosts.py            # Управление хостами
│   │   ├── resources.py       # Ресурсы (шаблоны, сниппеты)
│   │   ├── billing.py          # Биллинг
│   │   ├── system.py           # Системная информация
│   │   ├── navigation.py       # Навигация
│   │   ├── bulk.py             # Массовые операции
│   │   ├── common.py           # Общие утилиты
│   │   ├── errors.py           # Обработка ошибок
│   │   └── state.py            # Управление состоянием
│   ├── keyboards/              # Inline-клавиатуры
│   │   ├── main_menu.py        # Главное меню
│   │   ├── user_actions.py     # Действия с пользователями
│   │   ├── nodes_menu.py       # Меню нод
│   │   └── ...                 # Другие клавиатуры
│   ├── services/               # Сервисы
│   │   ├── api_client.py       # Клиент API Remnawave
│   │   └── webhook.py          # Webhook сервер (FastAPI)
│   └── utils/                  # Утилиты
│       ├── formatters.py       # Форматирование данных
│       ├── notifications.py    # Уведомления
│       ├── auth.py             # Аутентификация
│       ├── logger.py           # Логирование
│       └── i18n.py             # Интернационализация
├── locales/                    # Локализация
│   ├── ru/                     # Русский язык
│   │   └── messages.json
│   └── en/                     # Английский язык
│       └── messages.json
├── docker-compose.yml          # Конфигурация Docker Compose
├── Dockerfile                  # Определение Docker-образа
├── requirements.txt            # Python-зависимости
├── WEBHOOK_SETUP.md           # Инструкция по настройке webhook
└── README.md                   # Этот файл
```

---

## 🐳 Docker-образ

---

## 🔔 Webhook-уведомления

Бот поддерживает получение webhook-уведомлений от панели Remnawave о различных событиях:

- **Пользователи**: создание, изменение, удаление, отключение, истечение подписки
- **Ноды**: создание, изменение, удаление, потеря/восстановление соединения
- **HWID устройства**: добавление, удаление
- **Сервис**: события панели, попытки входа

Подробная инструкция по настройке доступна в [WEBHOOK_SETUP.md](WEBHOOK_SETUP.md)

---

## 🔧 Решение проблем

### Бот не отвечает

1. **Проверьте статус бота:**
   ```bash
   docker compose ps
   ```

2. **Проверьте логи на наличие ошибок:**
   ```bash
   docker compose logs -f bot
   ```

3. **Проверьте переменные окружения:**
   ```bash
   docker compose config
   ```

### Проблемы с подключением к API

1. Убедитесь, что `API_BASE_URL` указан правильно
2. Проверьте, существует ли Docker-сеть:
   ```bash
   docker network ls | grep remnawave-network
   ```
3. Для внешнего API убедитесь, что URL доступен и токен валиден

### Отказано в доступе

- Убедитесь, что ваш Telegram ID указан в переменной окружения `ADMINS`
- Получите свой ID, отправив сообщение [@userinfobot](https://t.me/userinfobot)

### Проблемы с webhook

- Проверьте, что `WEBHOOK_SECRET` в боте совпадает с `WEBHOOK_SECRET_HEADER` в панели
- Убедитесь, что URL webhook доступен из панели
- Проверьте логи на наличие ошибок аутентификации
- Подробнее в [WEBHOOK_SETUP.md](WEBHOOK_SETUP.md)

---

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! 

1. Сделайте форк репозитория
2. Создайте ветку для функции (`git checkout -b feature/amazing-feature`)
3. Внесите изменения и закоммитьте (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Этот проект лицензирован под MIT License - подробности в файле [LICENSE](LICENSE).

---

## 💬 Поддержка

По вопросам и проблемам создавайте [issue](https://github.com/case211/remnawave-admin/issues) на GitHub.

Добавляйтесь в чат TG - https://t.me/remnawave_admin

---

<div align="center">

**Сделано с ❤️ для управления Remnawave**

</div>
