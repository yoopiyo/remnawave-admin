version: "3.9"

services:
  bot:
    image: ghcr.io/case211/remnawave-admin:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    networks:
      - remnawave-network

networks:
  remnawave-network:
    external: true
